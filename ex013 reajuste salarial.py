salario = float(input('Qual o salario do funcionário ? R$'))
reajuste = salario + (salario * 15 / 100)
print('Após o reajuste de 15% o salário do funcionáruo que era de R${} será  R${}. '.format(salario, reajuste))
